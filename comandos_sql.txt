ALTERAR TIPO DE UMA COLUNA​
alter table `tabela` add campo varchar(100) default 's' not null after campo;

RENOMEAR UMA COLUNA
alter table aluno
CHANGE cpf cadastro_de_pessoas_físicas VARCHAR(30);

RENOMEAR UMA DAS TABELAS​
rename table tabela_x to tabela_y

CRIAR FK
create table  aluno_disciplina(
id int not null auto_increment,
cd_aluno int not null,
cd_disciplina int not null,
primary key (id),
constraint aluno_disciplina_aluno foreign key (cd_aluno) references aluno(id),
constraint aluno_disciplina_disciplina foreign key (cd_materia) references disciplina(id)
);

REMOVER  COLUNA X E ADICIONAR COLUNA Y​
alter table nome_tabela drop column coluna_x;
alter table nome_tabela add coluna_y;

CRIAR AS TABELAS ALUNO, TURMA, PROFESSOR E DISCIPLINA​
create table aluno (
id int not null auto_increment,
nome varchar(150),
dt_nascimento date,
cpf int,
sexo varchar(1),
primary key(id)
);

create table turma (
id int not null auto_increment,
nome varchar(100),
primary key(id)
);

create table professor (
id int not null auto_increment,
nome varchar(150),
dt_nascimento date,
cpf int,
sexo varchar(1),
primary key(id)
);

create table disciplina (
id int not null auto_increment,
nome varchar(100),
primary key (id),
);

CRIAR UM BANCO DE DADOS “AULA”​
create database aula